import { TruncateProjectDescrPipe } from './truncate-project-descr.pipe';

describe('TruncateProjectDescrPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateProjectDescrPipe();
    expect(pipe).toBeTruthy();
  });
});
